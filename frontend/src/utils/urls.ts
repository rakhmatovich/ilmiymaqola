export const DOMAIN = "http://127.0.0.1:8000"

export const SUBDOMAIN = DOMAIN + "/api/v1"

export const NEWS = SUBDOMAIN + "/news/"
export const NEWS_ITEM = SUBDOMAIN + "/news/{id}"
export const CATEGORIES = SUBDOMAIN + "/categories/"
